name: aoc_workspace
description: Advent of Code workspace with automation, benchmarking, and AI tooling support.
version: 0.1.0
publish_to: none

environment:
  sdk: '>=3.4.0 <4.0.0'

dependencies:
  args: ^2.5.0
  http: ^1.2.1
  dotenv: ^4.2.0

dev_dependencies:
  test: ^1.25.0
  lints: ^3.0.0
  benchmark_harness: ^2.2.0
